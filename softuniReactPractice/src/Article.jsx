export default function Article(props){
    return (
        <section>
            <h1>{props.h1}</h1>
            <h2>{props.h2}</h2>
            <p>{props.p}</p>
            <p>{props.p}</p>
        </section>

    );
}